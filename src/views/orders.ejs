<%- include("./partials/head") %> <%- include("./partials/header-adm") %>

<main class="container-adm">
  <div class="way">
    <h5><a href="/order">Pedidos</a></h5>
  </div>
  <!-- <div class="add-item">
    <form action="/order/create" method="get">
      <button type="submit">Adicionar</button>
    </form>
  </div> -->

  <!-- <div class="in-table">
    <input type="search" name="" id="" placeholder="Pesquisar..." />
    <img src="/img/lupa.png" alt="" />
  </div> -->

  <% if(listOrders.length> 0) { %>

  <div class="container-orders">
    <% for (let order of listOrders) { %>
    <div class="oders">
      <a id="open-modal">
        <h4><%= order.id %></h4>
        <h4><%= order.created_At %></h4>
        <h4><%= order.status %></h4>
      </a>
    </div>

  </div>

  <!-- Modal -->
  <div id="fade" class="hide"></div>
  <div id="modal" class="hide">
    <div class="modal-header">
      <h3>Produto do pedido</h3>
    </div>
    <% for(let product of order.Products){ %>
    <div class="modal-body">
      <span>Nome: </span>
        <%= product.name  %> 
        <span>Descrição: </span>
        <%= product.description  %> 
        <span>Preço: </span>
        <%= product.price  %> 
      </div>
      <% } %>  
    <div class="modal-footer">
      <button id="close-modal">Fechar</button>
    </div>
  </div>
  <% } %>


  <% } %> <%- include("./partials/pagination") %>
</main>

<%- include("./partials/footer-adm") %>
